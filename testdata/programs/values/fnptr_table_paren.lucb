func one(x: i64) -> i64: return x + 1
func two(x: i64) -> i64: return x + 2
let table: (func(i64) -> i64)[2] = [one, two]
pub func answer() -> i64:
    var acc: i64 = 0
    for f in table:
        acc = f(acc)
    return acc + 37
