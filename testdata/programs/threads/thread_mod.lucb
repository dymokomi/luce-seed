# answer: 42
import thread
import sync
var lock: sync.Mutex
var counter: @u32
func work(arg: void*?):
    lock.lock()
    counter += 1
    lock.unlock()
pub func answer() -> i64!:
    let h = try thread.spawn(work, none)
    try h.join()
    return i64(counter.load()) + 41
