# args: a b
pub func main(arguments: str[]) -> i32!:
    try io.stderr().write("direct\n")
    discard(io.stderr().write("in discard\n") catch failure: recover 0)
    let w = io.stderr()
    try w.write("via view\n")
    try io.stdout().write("to stdout\n")
    return 0
