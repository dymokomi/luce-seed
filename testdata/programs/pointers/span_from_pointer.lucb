pub func answer() -> i64!:
    let p = try alloc i64[3]
    defer free(p)
    p[0] = 10
    p[1] = 20
    p[2] = 30
    let fromptr = i64[](p.data, 2)
    return fromptr[0] + fromptr[1] + i64(fromptr.length) + 8
